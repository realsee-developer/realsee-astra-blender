Start Goal mode in this new session. First check whether this goal has already been created through /goal; continue it if so. Otherwise, use create_goal if available to establish the persistent objective below. Do not set a token budget unless I explicitly request one. If you cannot activate Goal mode, state the actual status and continue the modeling work you can perform; do not claim it is active. Carry out the modeling, inspection, and corrections. Do not stop at analysis, a plan, a blockout, or delivery of scripts alone. Mark the goal complete only when the deliverables satisfy the acceptance criteria below.

PROJECT LOCATION
Use the project folder selected for this session as PROJECT_ROOT. Resolve it from the session's workspace context and verify that it contains data/. All project paths in this specification are relative to PROJECT_ROOT; source filenames refer to files under data/. Do not hard-code a username, home directory, project folder name, or machine-specific executable path. If the workspace context does not identify the project folder unambiguously, ask me to select it.

OBJECTIVE
Reconstruct the actual space documented in data/ as a Blender-native scene that matches the scanned site and supports continued design edits. Deliver output/reconstruction_native.blend.
Completeness means accounting for every source and covering every evidenced space and identifiable object. Multiple file formats representing the same scene must produce only one set of scene objects. Do not present unobserved hidden structures as measured facts. Reconstruct the existing site faithfully; do not redesign it or substitute a sample house.

1. INSPECT THE SOURCES AND ENVIRONMENT, THEN PROCEED
Read the applicable AGENTS.md instructions and inspect the actual data files, installed Blender version, and available tools. Existing research/data-inventory.md, research/astra-blender-sources.md, and research/visual-and-environment-notes.md can help you locate evidence, but current source files and actual checks take precedence.

Known inputs include cad.dxf/cad.dwg; point-cloud.e57/point-cloud.ply; basic and high-resolution OBJ, FBX, glTF, and GLB models with textures under model; six model-orthogonal-image views; schematic-floorplan_floor_1.png; eight panorama JPGs; eight panorama-raw DNGs; 48 cube-map images; and gen-vr-video.mp4. Create a file-by-file inventory recording purpose, corresponding area, inspection status, and duplicate relationships. Do not silently skip a source because its format is inconvenient. Verified duplicate exports may share a processing result. Use DNGs for detail or exposure checks where needed, and the video to check spatial connections. Every file need not become a separate scene object.

Discover the installed Blender executable from the local environment or available Blender integration, and verify its version and connection status. You may build and render directly with Blender's bundled Python/bpy and its background --python execution, then open the actual .blend file for inspection. MCP is one control channel, not a prerequisite for completing the goal. Execute the work yourself rather than delivering code for me to run manually.

2. ESTABLISH SCALE, COORDINATES, AND SPATIAL CORRESPONDENCE
First extract outlines, annotations, doors, windows, and location-specific ceiling heights from the DXF entities, and cross-check them against E57/PLY, the high-resolution mesh, and photographs. The DXF contains $INSUNITS=4 and explicit millimeter notes; the schematic floor-plan PNG uses feet and inches. Normalize to meters in Blender. Record every source-to-scene transform and control point, and verify the floor datum, vertical axis, rotation, handedness or mirroring, and origin.

Do not assume the correct axis orientation from the glTF extension or infer camera positions from panorama numbering. Inspect E57 image/scan GUID associations, quaternions, and translations. External JPGs have not been verified as identical to the embedded E57 images; match image content and orientation before assigning camera poses. Curved lines caused by panorama projection are not evidence of curved walls. Use cube-map faces or rectified views to judge geometry.

The CAD annotations of 87.25 square meters, approximately 13203 x 9730 mm, and multiple CH values from 2470 to 3021 mm are verification clues. Determine their associated locations and measurement definitions. Do not force a uniform ceiling height or scale everything to a bounding box based on these numbers. CAD BLOCKS contain furniture, bathroom-fixture, and stair legend symbols. Only actually placed entities supported by site evidence belong in the reconstruction; legend symbols are not a site object inventory.

Use CAD annotations to constrain dimensions, point clouds and the high-resolution mesh to constrain actual 3D form, photographs to constrain visible details and appearance, and orthographic views and plans to cross-check the result. Locate and measure conflicts between sources, and explain which evidence you adopt and why. Do not manipulate scale, remove points, or ignore contradictions merely to obtain convenient numbers.

3. RECONSTRUCT THE ACTUAL SPACE ITEM BY ITEM
Create a coverage inventory and stable object IDs for rooms/areas, wall segments, doors/windows, fixed components, furniture, equipment, and contents. Store source references, area assignments, and measured/inferred/unknown status in object custom properties or a traceable mapping.

Eight panorama stations do not mean eight rooms. Initial observations show a gray-walled corridor and entrance, a white clothing/photography display room, a small bar, a purple foosball room, and a living/dining display area. Determine the actual spatial divisions from registered sources. Check all six directions at every station, including ceilings, lighting, floors, door backs, and corners.

Cover every evidenced wall, floor, area-specific ceiling/grid/cove, door/window and frame, skirting board, light fixture, air vent, switch, and outlet. Also cover sofas, tables, chairs, cabinets, shelving, televisions, bar shelving and bottles, clothing racks, garments, bags, shoes, photography lights, the foosball table, framed artwork, curtains, plants, and identifiable equipment and small objects. These examples are not an exhaustive inventory: check the actual sources item by item. Match object count, position, orientation, silhouette, and materials to the site. Do not reconstruct mirror or glass reflections as extra rooms or duplicate physical objects.

Complete the structure and major objects across the entire space before refining details and materials. Save staged checkpoints, but do not substitute placeholder boxes, single-view billboards, a panorama sphere, or automatically generated lookalike assets for the final reconstruction. For insufficient resolution, occlusions, or missing areas, record the source, confidence, and rationale for any inferred completion. Explicitly report substantial geometry gaps; do not count them as verified completion.

4. NATIVE EDITABILITY IS REQUIRED
Final visible structures and objects must use editable local Mesh and Curve data, native material nodes, and appropriate native modifiers stored in the .blend file. Split walls, doors, windows, furniture, and independently operable components into meaningful parts, give them semantic names, and organize Collections by floor/area/category. Separate door leaves from frames and place each door leaf's origin at its hinge. Important dimensions must be adjustable through object geometry, modifiers, or native nodes, rather than exclusively through an external generation script.

Maintain topology suitable for continued editing, actual thickness, and real openings. Use Boolean, Solidify, Bevel, Array, and Geometry Nodes where appropriate; using every technique is not required. Importing, decimating, or renaming an entire triangulated scan does not fulfill the goal. Rebuild, separate, and clean up topology as needed for the objects that must be independently editable. Complex objects may reuse existing scanned shapes, provided they satisfy the part-separation and practical editing checks.

Preserve the unmodified high-resolution scan reference, original textures, and registration information in a clearly named Reference collection or a separate reference.blend. Exclude references from final rendering by default. The reconstructed native scene must remain complete when references are hidden; the scan layer must not conceal missing reconstruction. Do not stack duplicate basic/high representations from different formats.

Materials must be independently editable. UVs and image textures are valid: artwork, labels, and other genuinely flat graphics may use textures, while frames and supporting objects require correct geometry. Do not replace 3D structure with a baked flat image. Separate captured illumination from material base color where practical. The final model must open, remain editable, and render without MCP, network access, or additional modeling plugins.

5. CONTINUE EXECUTION AND VERIFY COMPLETION
Maintain progress, the object coverage inventory, source mappings, and key decisions. Save .blend checkpoints and reproducible scripts at meaningful milestones. Delegate independent source parsing, object identification, asset modeling, and review to subagents when parallel work helps. The primary agent owns scale consistency and integration. Do not let multiple agents write the same .blend or control the same Blender scene concurrently.

At each stage, inspect actual Blender output relevant to the changes just made. Start with inexpensive previews and correct identified issues. Final verification must cover untextured Solid views, wireframes, plan/elevation views, interiors of every area, and camera views registered to all eight stations. Hide the original scan reference during these checks. One attractive render is insufficient. Compare against source photographs for omissions, misalignment, intersections, proportions, materials, and lighting.

Before delivery, complete all of the following:

1. Account for every substantive source file with a use, verification, or duplicate-mapping record. Map every space, opening, and identifiable object in the coverage inventory to actual scene objects. List unresolved items explicitly. Do not shrink the inventory to manufacture a claim of 100% coverage. Do not mark the goal complete while observable objects are missing, solvable errors remain, or required verification is unfinished. Hidden details unsupported by evidence may use documented reasonable inferences; you are not required to prove unobservable facts.

2. Report reference dimensions, model dimensions, and differences for key measurements. For clearly matched, non-conflicting CAD annotations, target a model dimensional discrepancy of no more than 10 mm. This is a modeling target, not a claim about scan measurement accuracy. For reliable wall, floor, and ceiling surfaces, report registered point-cloud distance statistics and the sampling method. Distinguish noise, missing observations, and genuine deviations; interpret errors in light of actual source quality and do not invent precision.

3. Check missing textures, broken dependencies, incorrect normals, degenerate faces, non-manifold structural solids, and obvious intersections. Apply checks according to object purpose; do not mechanically require legitimate open surfaces such as fabric or leaves to be closed solids.

4. Reopen the final file from disk in a fresh Blender process. In a temporary copy, actually perform, save, and reopen these edits: change a wall segment or opening dimension; rotate a door around its hinge; move one furniture item independently; and change that item's material. For a representative complex asset that reuses scanned geometry, also modify local geometry in Edit Mode, save, and reopen it. Object movement alone does not prove useful editability. Verify that other objects remain correct and provide evidence. Deliver the reconstruction in its original intended state, without the temporary acceptance-test changes.

5. Confirm the scene is complete with Reference disabled. Pack the final material resources or provide them with the delivery folder through verified relative paths; reopening and rendering must reveal no missing resources. Large original photos, RAW files, and point clouds may remain in data and be tracked through the inventory. All resources needed to render the delivered model must accompany it.

DELIVERABLES AND AUTHORIZATION
Deliver output/reconstruction_native.blend, any necessary reference.blend/assets, the scripts actually executed, structural and area-by-area comparison previews, an acceptance report covering data mappings, errors, coverage, editing verification, and uncertainties, plus concise editing instructions.

I authorize the local parsing, modeling, saving, rendering, and verification described above. Preserve original data files and existing user work. Make routine reversible technical decisions autonomously. Ask a specific question only when missing information materially affects scope or correctness, or an actual permission boundary prevents progress; continue independent work in the meantime. Do not commit or push code, upload source data, publish, make purchases, or send external messages. Continue until acceptance is complete. If a genuine source-data or tool blocker cannot be resolved, deliver the current artifacts and identify the specific gaps without falsely marking the goal complete.
